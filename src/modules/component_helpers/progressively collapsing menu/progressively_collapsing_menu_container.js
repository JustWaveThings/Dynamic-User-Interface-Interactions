// create the container that holds the top level menu items that is reusuable for all menus of the same style.

import toggle from './toggleClickedElement';

// the anchor ul element has .nav-list as a class
